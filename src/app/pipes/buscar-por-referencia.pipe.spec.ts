import { BuscarPorReferenciaPipe } from './buscar-por-referencia.pipe';

describe('BuscarPorReferenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPorReferenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
